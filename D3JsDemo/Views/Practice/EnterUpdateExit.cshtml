@{
    ViewBag.Title = "EnterUpdateExit";
}

<html>
<body>

    <div id="container"></div>

    <script>

        var data = [10];

        var svg = d3.select("#container")
                    .append("svg")
                    .attr("width", 500)
                    .attr("height", 500);

        var circle1 = svg.append("circle")
                            .attr("cx", 50)
                            .attr("cy", 100)
                            .attr("r", 25)
                            .attr("fill", "red");

        var circle2 = svg.append("circle")
                            .attr("cx", 50)
                            .attr("cy", 200)
                            .attr("r", 25)
                            .attr("fill", "red");

        var circle = svg.selectAll("circle")
                        .data(data)
                        .attr("fill", "green")
                        .exit()
                            .attr("fill", "blue");



    </script>
</body>
</html>